# Frontend env (Vite requires VITE_ prefix)
# For local dev the vite proxy handles /api and /ws, so you can leave these empty.
# For deployed builds, set the backend URL:
# VITE_API_URL=https://api.example.com
# VITE_WS_URL=wss://api.example.com/ws
